# constCS.py
# Constantes de rede compartilhadas entre Cliente e Servidor

HOST = '127.0.0.1'  # Endereço local (localhost). Para testar entre máquinas diferentes, use o IP da máquina servidora.
PORT = 5678         # Porta do serviço (não privilegiada, > 1024)
